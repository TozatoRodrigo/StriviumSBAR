JWT_SECRET=tvPr8uB7nwTI06NNAQD8purY259oqvkr

# Disable global rate limiting in tests so unrelated suites are not throttled.
# The rate limiting behaviour itself is covered by an isolated, opt-in test.
RATE_LIMIT_ENABLED=false

DB_DRIVER=sqlite
DB_HOST=:memory:
DB_PORT=1234
DB_NAME=strivium_api
DB_USER=strivium_api
DB_PASSWORD=strivium_api
DB_DEBUG=false

ENABLE_DOCS=true

APP_MODULE=

FILESYSTEM_DRIVER="fake"

TURNSTILE_ENABLED=true
CLOUDFLARE_TURNSTILE_SECRET='turnstile_secret'